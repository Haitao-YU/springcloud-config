# config-test-dev
server:
  port: 3344


spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri:  git@github.com:Haitao-YU/springcloud-config.git   # github上仓库的名字
          search-paths:
            - springcloud-config
      label: main


# 注册进入eureka服务中心
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka,http://localhost:7002/eureka

